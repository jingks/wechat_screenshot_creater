{
  "name": "screenshot-creater",
  "version": "1.0.0",
  "description": "a web to create wechat screenshot",
  "main": "index.js",
  "scripts": {
    "test": "echo \"ok this is only a test\" && exit 1",
    "clean": "rmdir /S /Q dist && mkdir dist",
    "copy": "xcopy /E /H /EXCLUDE:.exclude src dist",
    "sass": "sass --update src/sass:src/css",
    "sass:watch": "start sass --watch src/sass/main.scss:src/css/main.css",
    "start": "start node server.js & start http://127.0.0.1:80 & exit"
  },
  "author": "jingks",
  "license": "ISC",
  "dependencies": {
    "cnpm": "^6.1.1",
    "express": "^4.17.3"
  }
}
